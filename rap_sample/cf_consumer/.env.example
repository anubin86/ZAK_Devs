# Local dev only – never commit to source control
# In CF, these values come from the bound Destination service

# Direct connection (used when no CF Destination service is available)
RAP_BASE_URL=https://your-s4-system.example.com
RAP_USERNAME=your_user
RAP_PASSWORD=your_password

PORT=3000
