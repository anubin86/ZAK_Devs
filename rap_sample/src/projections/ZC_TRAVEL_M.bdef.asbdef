projection;
strict ( 2 );
use draft;

define behavior for ZC_TRAVEL_M alias Travel
{
  use action acceptTravel;
  use action rejectTravel;
  use action copyTravel;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}
